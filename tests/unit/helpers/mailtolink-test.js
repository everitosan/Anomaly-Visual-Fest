import { mailtolink } from '../../../helpers/mailtolink';
import { module, test } from 'qunit';

module('Unit | Helper | mailtolink');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mailtolink([42]);
  assert.ok(result);
});
